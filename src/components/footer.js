import React from "react";

export default function Footer() {
  return (
    <div
      style={{
        display: "flex",
        flexDirection: `column`,
        alignItems: "center",
        margin: "25px",
      }}
    >
      FOOTER
    </div>
  );
}
